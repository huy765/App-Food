import * as React from 'react';
import * as PropTypes from 'prop-types';
import { IPieLabel } from 'goblin-base/es/plugin/pieLabel';
declare class Props {
}
export default class PieLabel extends React.Component<Props & IPieLabel, any> {
    static contextTypes: {
        centralizedUpdates: PropTypes.Requireable<(...args: any[]) => any>;
    };
    displayName: string;
    constructor(props: Props & IPieLabel);
    componentDidMount(): void;
    componentDidUpdate(): void;
    render(): any;
}
export {};
